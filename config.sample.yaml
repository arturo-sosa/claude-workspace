# Workspace Configuration

# Git identity for commits in worktrees
# Falls back to git config user.name/user.email if not set
git:
  #   user: "git-username"
  #   email: "user@github.com"

# Run /workspace-repos to refresh the projects inside ./repos
repos:
  # - name: frontend
  #   url: git@github.com:org/frontend.git
  #   branch: main
